package Expr;

import java.io.IOException;

import org.antlr.v4.runtime.ANTLRFileStream;
import org.antlr.v4.runtime.CommonTokenStream;

import Simple.SimpleBaseListener;
import Simple.SimpleLexer;
import Simple.SimpleParser;
import Simple.SimpleParser.PrintlnContext;

public class Main {
  /*
  public static final String EXTENSION = "smp";

  public static void main(final String[] args) throws IOException {
    String prog = args.length > 1 ? args[1] : "test/test." + EXTENSION;
    System.out.println("iniciando " + prog);
    SimpleLexer lexer = new SimpleLexer(new ANTLRFileStream(prog));
    CommonTokenStream tokens = new CommonTokenStream(lexer);
    SimpleParser parser = new SimpleParser(tokens);
  SimpleParser.ProgramContext   tree = parser.program();
ExprVisitor visitor = new ExprVisitor();
visitor.visit(tree);

System.out.println("Finalizando Interpretación");
*/


   
        
    
    
}